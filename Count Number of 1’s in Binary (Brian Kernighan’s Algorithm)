#include <bits/stdc++.h>
using namespace std;
int countBits(int n) {
    int count = 0;
    while (n) {
        n &= (n - 1);
        count++;
    }
    return count;
}
int main() {
    int n = 29; // 11101
    cout << "Count of 1s: " << countBits(n);
}
